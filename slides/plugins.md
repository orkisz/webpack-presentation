##  Plugins

Plugins add additional functionality. They are usually extending bundling proccess with additional tasks. There are 3rd party and webpack also has couple of built-in plugins.

Unlike loaders, plugins have access to entire build pipeline.

